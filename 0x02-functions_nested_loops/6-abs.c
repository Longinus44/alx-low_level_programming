#include "main.h"
/**
 * _abs - function that computes absolute value
 * @i: number to check
 *
 * Return: 0 (Success)
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
